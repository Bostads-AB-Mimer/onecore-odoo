<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">

    <t t-name="web.AccordionRecord">
        <div
        role="article"
        t-att-class="getRecordClasses()"
        t-att-data-id="props.record.id"
        t-att-tabindex="props.record.model.useSampleModel ? -1 : 0"
        t-on-click="onGlobalClick"
        t-ref="root">
            <t t-call="{{ templates[this.constructor.ACCORDION_BOX_ATTRIBUTE] }}" t-call-context="this.renderingContext"/>
            <t t-call="{{ this.constructor.menuTemplate }}"/>
        </div>
    </t>
    <t t-name="web.AccordionRecordMenu">
        <t t-if="showMenu">
            <Dropdown class="'o_dropdown_accordion bg-transparent position-absolute end-0'"
                      menuClass="'dropdown-menu'"
                      togglerClass="'btn o-no-caret rounded-0'"
                      position="'bottom-end'"
                      title="'Dropdown menu'">
                <t t-set-slot="toggler">
                    <span class="fa fa-ellipsis-v"/>
                </t>
                <!-- <AccordionDropdownMenuWrapper>
                    <t t-call="{{ templates[this.constructor.ACCORDION_MENU_ATTRIBUTE] }}"
                t-call-context="renderingContext"/>
                </AccordionDropdownMenuWrapper> -->
            </Dropdown>
        </t>
    </t>
</templates>